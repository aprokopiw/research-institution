# Supported Python versions for the canonical simulation.
# Per entry 08 FR-4 + M1 T1.2, this file is the single source of
# truth for the supported Python matrix.
#
# ``minimum``  — lowest version still supported.
# ``current``  — version the operator runs today.
# ``unsupported`` — versions the runner reports as BLOCKED.
#
# The CompatMatrixRunner reads this file; a missing file falls
# back to ``minimum=3.12, current=<interpreter>``.

minimum = "3.12"
current = "3.14"
unsupported = ["3.10", "3.11"]
