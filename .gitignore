# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.ruff_cache/
.hypothesis/
.import_linter_cache/

# Virtual environments
.venv/
venv/
env/

# Build artifacts
build/
dist/
*.egg

# Plan artifacts (transient; never tracked)
.agents/transient/
.pi-glla/
# Per-session handoff (continues across chat resets within one bring-up,
# but rots across plans / operator handoffs). Keep on disk, never commit.
context_handoff.md

# OS
.DS_Store
*.swp
*~

# Editor
.idea/
.vscode/

# Local override files
mathlint.local.toml
*.local.toml
